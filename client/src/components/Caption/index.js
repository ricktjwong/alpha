import React from 'react';

const Caption = (weights) => {
  return (
    <div>
      Returns from the following allocation of weights {JSON.stringify(weights)}
      .
    </div>
  );
};

export default Caption;
